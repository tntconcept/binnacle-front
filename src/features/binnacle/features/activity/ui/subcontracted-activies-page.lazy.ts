import { lazy } from 'react'

export const LazySubcontractedActivitiesPage = lazy(() => import('./subcontracted-activities-page'))

/*
import { lazy } from 'react'

export const LazyActivitiesPage = lazy(() => import('./activities-page'))

*/
